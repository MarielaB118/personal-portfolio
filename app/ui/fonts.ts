import { Raleway, Cormorant, Brawler } from "next/font/google";

export const raleway = Raleway({ subsets: [ 'latin' ], weight: '400' });
export const cormorant = Cormorant({ subsets: [ 'latin' ], weight: '400' });
export const brawler = Brawler({ subsets: [ 'latin' ], weight: '400' });